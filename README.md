# VCR - event recorder and replay

# use custom configuration (recompile and run custom program)

```bash
vcr custom --program "/path/to/program.hs --args..." --ghcOpts "-i/path/to/other/includes"
```

